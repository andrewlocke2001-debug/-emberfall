# Postgres connection string (Neon) — required. Get yours from the Neon
# dashboard ("Connect"). Same variable is set as a Fly secret in production.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Server port (defaults to 2567):
# PORT=2567
